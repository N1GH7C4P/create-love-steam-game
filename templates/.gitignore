# Build output
dist/
.love-build/
.build-staging/
src/version.lua

# Local secrets — never commit
.env

# OS clutter
.DS_Store
Thumbs.db

# Log output
logs/

# Steam local dev override — use 480 (Spacewar) during development
# Your real App ID lives in scripts/deploy-steam.sh, not here
steam_appid.txt
