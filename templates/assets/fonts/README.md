# Fonts

Place `.ttf` or `.otf` font files here.

Load them in Lua with:

```lua
local my_font = love.graphics.newFont("assets/fonts/MyFont.ttf", 16)
```

The default template uses Love2D's built-in font (no file needed).
